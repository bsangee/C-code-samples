int gcd(int A, int B) {
    
    int i,j;
    int n= A < B ? A : B;
    if(n!=0){
    for(i=n;i>=2;i--){
        if(A % i ==0 && B%i == 0){
            return i;
        }
    }
    }
    else{
        return A < B ? B:A;
    }
    return 1;
}
